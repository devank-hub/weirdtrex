import * as Discord from "discord.js";
import { bot } from "../../../api";

export default function unhandledPresenceUpdate(client: bot, oldMember: Discord.GuildMember, newMember: Discord.GuildMember){

}